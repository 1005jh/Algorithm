const arr = [-4, 3, -9, 0, 4, 1];

function plusMinus(arr) {
  let answer = 0;
  let minuSArr = arr.sort((a, b) => a - b).join("");

  const minuSplitArr = minuSArr.split("0");
  console.log(minuSplitArr);
  const minus = minuSplitArr[0].split("");
  let nagative;
  for (let i = 0; i < minus.length; i++) {
    if(minus !==)
  }
  console.log(nagative);
  //   for (let i = 0; i < splitArr.length; i++) {}
  return answer;
}
console.log(plusMinus(arr));

//*positive :
//*nagative :
//*zeros :
